#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: integer
 * @b: other integer
 * Return: the value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
